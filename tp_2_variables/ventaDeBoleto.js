let nombre = 'Matias';
let apellido = 'Flores';
let edad = 20;
let numDeTelefono = 1156872302;
let socio = true;
let fechaDelPartido = "23 de Julio de 2024";
let horaDelPartido = "20:30";
let numeroDeSilla = 14;
let precioDelBoleto = 99.50;

console.log(`Nombre completo del socio: ${nombre} ${apellido} (${typeof nombre, typeof apellido})`);

console.log(`Edad del socio: ${edad} (${typeof edad})`);

console.log(`Número de Telefono del socio: ${numDeTelefono} (${typeof numDeTelefono})`);

console.log(`¿Es socio actualmente? ${socio} (${typeof socio})`);

console.log(`Boleto:
     Fecha del partido: ${fechaDelPartido} (${typeof fechaDelPartido})
     Hora del partido: ${horaDelPartido} (${typeof horaDelPartido})
     Numero de silla: ${numeroDeSilla} (${typeof numeroDeSilla})
     Precio del boleto: $${precioDelBoleto} (${typeof precioDelBoleto})`);








